@page "/generation/{Generation:int}"
@using BlazorPokedex.Logic;

@if (Pokemons == null)
{
    <Loading/>
}
else
{
    <div id="title"><h1>Generation @Generation Pokemons </h1></div>

    <div id="pokemon-list">
        @foreach(var pokemon in Pokemons)
        {
            <div class="pokemon-list-item list-item">
                <a href=@($"pokemon/{pokemon.Id}")>
                    <div class="image"><img src=@pokemon.ImageUrl /></div>
                    <div class="number">@($"Nº {pokemon.Id:000}")</div>
                    <div class="name">@pokemon.Name</div>
                    <div class="type-list">
                        @foreach(var type in pokemon.Types)
                        {
                            <PokemonTypeSlot Type=@type />
                        }
                    </div>
                </a>
            </div>
        }
    </div>
}
